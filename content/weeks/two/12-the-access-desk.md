---
title: "The access desk"
id: "I12"
shift: 12
weight: 12
subtitle: "the concierge, in the desk's form"
summary: "the concierge, in the desk's form"
today: "Put your co-hire on the desk. The estate is on screen (`chant search`); a tickets engineer says \"tickets-api needs read on the receipts bucket\"; `wp-request` makes one deterministic leaf edit; the plan is the rendered access delta; the PR is opened with a token that can do nothing else; the merge is the approval, never a message in the conversation (decision 18). An unmapped requester gets a refusal naming the enrollment path, and no PR."
done_when: "`chant run wp-request` with typed args produces the same PR with no agent; scenario 6 yields a one-file PR passing lint and the proof with no human edit; an unmapped identity gets a refusal and no PR; a boundary exception gets a directed refusal."
clock_in: "week one, shifts 4, 7, 8; shift 6"
rule: "Named secrets (handbook V); escalate the judgment (VIII); attributable (IX)."
properties: ["V", "VIII", "IX"]
closes: ["P13"]
---

## Steps

1. `chant run wp-request` with typed args produces the PR with no agent (D0).
2. The Environment manifest (chant-lexicon-fountain can declare it); `fountain apply` stands up the desk as a teammate (D1): checkout, SKILL.md, default-deny egress naming the verb service and the code host, no cloud credentials.
3. "tickets-api needs read on the receipts bucket" (scenario 6): one-file PR, lint and the proof pass, access delta rendered by the deterministic renderer, not the model.
4. An unmapped identity: a refusal naming the enrollment route, no PR. A boundary exception: a directed refusal to the platform path.
5. Add this desk's rows to shift 4's key table (the PR-only token, the gated apply role, what the sandbox holds) and fill the parts table from [the propose loop](../../propose-loop.md).

## Self-paced

Floci plus a Fountain instance; the verb service runs beside it. On your own truck (week one, shift 10) the wristband claim is void; say so.

## With the shift lead

Hosted sandbox provider; the Environment manifest on screen is beat 1 of every playlist. The punchline: the co-hire that built the repo in shift 1 just used it, and nobody trusted it at any point.

## Back office

[the propose loop](../../propose-loop.md); [agentic](../../docs/design/agentic.md); decisions 14, 15, 17, 18, 30; dns-desk README; Mend README.
