---
title: "Walk away"
number: "I15"
weight: 16
theme: "Accessible Ops I and XII (adopt in place). The estate outlives the toolchain: build the bundle, delete chant, deploy with the AWS CLI alone. And the reverse: reference-existing means day one touches nothing."
summary: "Accessible Ops I and XII (adopt in place). The estate outlives the toolchain: build the bundle, delete chant, deploy with the AWS CLI alone. And the reverse: reference-existing means day one touches nothing."
properties: ["I", "XII"]
builds_on: ["I6"]
---

## Outcome

The export bundle (A21): synthesized CloudFormation, policy
JSON, marker inventory, provenance; the adoption seams (A14).

## Steps

1. Build pepperoni's estate; export.
2. Remove the kit and every chant dependency; deploy the bundle with the
   AWS CLI; diff: byte-identical.
3. Adopt a pre-existing role and SG with reference-existing and zero
   composite edits.
4. If step 2 cannot run, decision 2 gets softened, in the same PR.

## Done when

A21's and A14's acceptance criteria.

## Solo

Floci.

## Live

Real; the optional closer of the long session.

## Depth

decision 2; [landscape.md](../../docs/landscape.md) (IAMbic).
