# envs/dev

`waterpark-dev` is the same shapes as `prod` with no traffic, where a change
lands first. It is empty until a lesson needs a second environment. Copy
`../prod` and change `env` when that happens.
