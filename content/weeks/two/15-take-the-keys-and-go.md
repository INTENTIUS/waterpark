---
title: "Take the keys and go"
id: "I15"
shift: 15
weight: 15
subtitle: "walk away: the export bundle"
summary: "walk away: the export bundle"
today: "Build the bundle, delete chant and the kit, deploy with the AWS CLI alone, diff: byte-identical. And the reverse: adopt a pre-existing role and SG with reference-existing, touching nothing. The park outlives the toolchain."
done_when: "The bundle deploys byte-identical without the toolchain; adopting a pre-existing role and SG needs zero composite edits."
clock_in: "shift 6"
rule: "Honor the lower layer (handbook I); adopt in place (XII)."
properties: ["I", "XII"]
---

## Steps

1. Build splashdown's estate; export (A21): synthesized CloudFormation, policy JSON, marker inventory, provenance.
2. Remove the kit and every chant dependency; deploy the bundle with the AWS CLI; diff: byte-identical.
3. Adopt a pre-existing role and SG with reference-existing and zero composite edits (A14).
4. If step 2 cannot run, decision 2 gets softened, in the same PR.

## Self-paced

Floci.

## With the shift lead

Real; the optional closer of the long session.

## Back office

decision 2; [landscape](../../docs/landscape.md) (IAMbic).
